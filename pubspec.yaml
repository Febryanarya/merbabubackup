name: merbabuaccess_app
description: A Merbabu hiking booking application.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.2.0
  fluttertoast: ^8.2.4
  google_sign_in: ^6.1.5

  # Utils
  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  http: ^1.2.1
  intl: ^0.19.0

  # Storage
  shared_preferences: ^2.2.2
  
  # QR Code (FIXED VERSION)
  qr_flutter: ^4.1.0
  
  # Media
  image_picker: ^1.0.4
  
  # Untuk Responsive (Nanti)
  # flutter_screenutil: ^5.9.0
  # responsive_framework: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Assets
  assets:
    - assets/images/thekelan.jpg
    - assets/images/wekas.jpg
    - assets/images/suwanting.jpeg

  generate: true